/**
* @file:   proto/taiyangneng_data.proto
* @author: jameyli <jameyli AT tencent DOT com>
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

package com.game.framework.resource.data;

message TAIYANGNENG{
    /** 索引 */
    required int32 id = 1 [default = 0];
    /** 发电速率 */
    optional int32 taiyangneng_spd = 2 [default = 0];
    /** 单次最大储电量 */
    optional int32 taiyangneng_cap = 3 [default = 0];
}

message TAIYANGNENG_ARRAY {
    repeated TAIYANGNENG items = 1;
}
