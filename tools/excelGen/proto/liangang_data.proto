/**
* @file:   proto/liangang_data.proto
* @author: jameyli <jameyli AT tencent DOT com>
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

package com.game.framework.resource.data;

message LIANGANG{
    /** 索引 */
    required int32 id = 1 [default = 0];
    /** 炼钢速度 */
    optional int32 liangang_spd = 2 [default = 0];
    /** 最高炼钢量 */
    optional int32 liangang_cap = 3 [default = 0];
}

message LIANGANG_ARRAY {
    repeated LIANGANG items = 1;
}
