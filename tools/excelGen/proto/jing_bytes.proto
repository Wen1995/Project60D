/**
* @file:   proto/jing_bytes.proto
* @author: jameyli <jameyli AT tencent DOT com>
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

package com.game.framework.resource.data;

message JING{
    /** 索引 */
    required int32 id = 1 [default = 0];
    /** 渗水速度 */
    optional int32 jing_spd = 2 [default = 0];
    /** 容量 */
    optional int32 jing_cap = 3 [default = 0];
}

message JING_ARRAY {
    repeated JING items = 1;
}
